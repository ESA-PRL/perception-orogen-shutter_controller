# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(shutter_controllerTaskLib)
ADD_LIBRARY(${SHUTTER_CONTROLLER_TASKLIB_NAME} SHARED 
    ${SHUTTER_CONTROLLER_TASKLIB_SOURCES})
add_dependencies(${SHUTTER_CONTROLLER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SHUTTER_CONTROLLER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SHUTTER_CONTROLLER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SHUTTER_CONTROLLER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SHUTTER_CONTROLLER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${SHUTTER_CONTROLLER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${SHUTTER_CONTROLLER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SHUTTER_CONTROLLER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SHUTTER_CONTROLLER_TASKLIB_HEADERS}
    DESTINATION include/orocos/shutter_controller)

